<div>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
          <div class="container-fluid">
              <a class="navbar-brand" href="/" style="font-family:Market Sans; font-size: 25px;">Aardvark</a>
              <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
                  aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarResponsive">
                  <div>
                      <header id="search-bar" style=>
                          <form method="post" action="/searchResults">
                              <div class="search-inner px-3">
                                  <div class="input-group mb-2">
                                      <div class="input-group-prepend">
                                          <!-- Save previous search queries, if any -->
                                            <% let dropdown = ["texts","school supplies","home goods"]; %>
                                                <select name="type">
                                            <% if (typeof type != 'undefined') { %>
                                                <option value="" selected>All</option>
                                            <% } else { %>
                                                <option value="" >All</option>
                                            <% } %>
                                            <% for ( let i = 0; i < dropdown.length; i++) { %>
                                                <% if (typeof type != 'undefined' && type == dropdown[i]){ %>
                                                    <option value="<%= dropdown[i] %>" selected><%= dropdown[i] %></option>
                                                <% } else { %>
                                                    <option value="<%= dropdown[i] %>"><%= dropdown[i] %></option>
                                                <% } %>
                                            <% } %>
                                          </select>
                                      </div>
                                      <!-- Search Bar -->
                                      <% let search = ""; if (typeof searchTerm != 'undefined') search = searchTerm; %>
                                  <input id="search" name="search" type="text" class="form-control" placeholder="Enter here to search" value="<%= search %>" />
                                  <span class="input-group-prepend">
                                  <button class="btn btn-warning" style="border-radius: 3px;" type="submit">
                                      <i class="fa fa-search fa-lg" aria-hidden="true"></i>
                                  </button>
                                  </span>

                                  </div>
                              </div>
                          </form>
                      </header>
                  </div>
                  <!-- Other nav bar items -->
                  <ul class="navbar-nav ml-auto">
                    <!--Need to link these pages-->
                    <% if (loggedin) {%>
                    <li class="nav-item  <% if (page === 'dashboard') { %>active <% } %>">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item  <% if (page === 'post') { %>active <% } %>">
                        <a class="nav-link" href="/post">Post</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                    <% } else { %>
                    <li class="nav-item  <% if (page === 'login') { %>active <% } %>">
                        <a class="nav-link" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item  <% if (page === 'register') { %>active <% } %>">
                        <a class="nav-link" href="/auth/register">Register</a>
                    </li>
                    <% } %>
                  </ul>
              </div>
          </div>
      </nav>