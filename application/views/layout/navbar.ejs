<div>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
          <div class="container-fluid">
              <a class="navbar-brand" href="/">Aardvark</a>
              <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
                  aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarResponsive">
                  <div>
                      <header id="search-bar" style=>
                          <form method="post" action="searchResults">
                              <div class="search-inner px-3">
                                  <div class="input-group mb-2">
                                      <div class="input-group-prepend">
                                          <select name="type">
                                          
                                              <option value="">All</option>
                                              <option value="texts" <%= type == 'texts' ? 'selected' : '' %>> Texts </option>
                                              <option value="school supplies" <%= type == 'school supplies' ? 'selected' : '' %>> School Supplies </option> 
                                              <option value="home goods" <%= type == 'home goods' ? 'selected' : '' %>> Home Goods </option>

                                              <!-- <option value="texts" <%= type == 'texts' ? 'selected' : '' %>>Texts </option> -->
                                              <!-- <option value="school supplies" <%= type == 'school supplies' ? 'selected' : '' %>> School Supplies </option> -->
                                              <!-- <option value="home goods" <%= type == 'home goods' ? 'selected' : '' %>> Home Goods </option> -->

                                          </select>
                                      </div>
                                      <!--Search Bar-->
                                      <% let searchTerm = ""; %>
                                  <input id="search" name="search" type="text" class="form-control" placeholder="Enter here to search" value="<%= searchTerm %>" />
                                  <span class="input-group-prepend">
                                  <button class="btn btn-warning" style="border-radius: 3px" type="submit">
                                      <i class="fa fa-search fa-lg" aria-hidden="true"></i>
                                  </button>
                                  </span>

                                  </div>
                              </div>
                          </form>
                      </header>
                  </div>
                  <!-- Other nav bar items -->
                  <ul class="navbar-nav ml-auto">
                    <!--Need to link these pages-->

                    <li class="nav-item  <% if (page === 'dashboard') { %>active <% } %>">
                        <a class="nav-link" style="color: white;" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item  <% if (page === 'sell') { %>active <% } %>">
                        <a class="nav-link" style="color: white;" href="/sell">Post</a>
                    </li>
                    <li class="nav-item  <% if (page === 'login') { %>active <% } %>">
                        <a class="nav-link" style="color: white;" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item  <% if (page === 'register') { %>active <% } %>">
                        <a class="nav-link" style="color: white;" href="/auth/register">Register</a>
                    </li>
                    <li class="nav-item  <% if (page === 'register') { %>active <% } %>">
                        <a class="nav-link" style="color: white;" href="/auth/register">Register</a>
                    </li>
                  </ul>
              </div>
          </div>
      </nav>