<!doctype html>
<html lang="en">

<% include layout/header %>

<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

<body>
    <% include layout/navbar %>
    <div class="container-fluid">
        <div id="row-align" class="row justify-content-around">

            <div class="col-sm-4">
                <img class="img-fluid" src="data:itemImage/jpg;base64,<%= img %>" alt="<%= item[0].name %>" />
            </div>

            <div class="col-sm-4 ">
                <!-- TO ACCESS item ARRAY FROM BACKEND.  -->
                <!-- <h1><%= item[0].name %></h1>
                <h1><%= item[0].price %></h1>
                <img src="data:itemImage/jpg;base64,<%= img %>" alt="<%= item[0].name %>" class="" style="width:60px;"/> -->
                <!-- ONLY 1 OBJECT HENCE INDEX[0] -->

                <!-- <div class=" ml-auto"><a href="#" class="btn btn-warning">Go somewhere</a></div> -->
                <div>
                    <h1><%= item[0].name %></h1>
                    <p>
                        <%= item[0].description %>
                    </p>

                    <h4>$<%= item[0].price %>.00</h4>
                    <button style="border-radius:0;" onclick="message(<%= item[0].id %>)" type="button"
                        class="btn btn-warning">Contact Seller</button>
                </div>
            </div>

        </div>
    </div>
    <% include layout/footer %>
</body>

</html>